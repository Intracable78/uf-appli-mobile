export interface Register {
    email: string;
    password: string;
    name: string;
    lastName: string;
    birthDate: string;
    address: string;
    country: string;
    phone: string;
    id?: string;
    firebaseId?: string;
}